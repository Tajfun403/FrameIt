<Page x:Class="FrameIt.UI.MainUI"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FrameIt.UI"
      xmlns:general="clr-namespace:FrameIt.General"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{DynamicResource MaterialDesignPaper}"
      FontFamily="{DynamicResource MaterialDesignFont}"
      Title="MainUI">

    <materialDesign:DialogHost
    Identifier="RootDialog"
    OverlayBackground="{DynamicResource MaterialDesignScrim}">

        <Grid Background="{DynamicResource MaterialDesignPaper}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <general:NavigationHeader Grid.Row="0" />
            <Frame Grid.Row="1" Name="MainContentFrame" Source="/Shows/ShowsMain.xaml" NavigationUIVisibility="Hidden"/>
            <materialDesign:Snackbar
             Grid.RowSpan="2"
            Background="#222"
            Foreground="#fff"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
            x:Name="MainSnackbar"
            MessageQueue="{materialDesign:MessageQueue}" />
            <materialDesign:Snackbar
             Grid.RowSpan="2"
            Background="#6A2A2A"
            Foreground="#fff"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
            x:Name="ErrorSnackbar"
            MessageQueue="{materialDesign:MessageQueue}" />
            <materialDesign:Snackbar
             Grid.RowSpan="2"
            Background="DarkGreen"
            Foreground="#fff"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
            x:Name="SuccessSnackbar"
            MessageQueue="{materialDesign:MessageQueue}" />
        </Grid>
    </materialDesign:DialogHost>
</Page>
