<UserControl x:Class="FrameIt.General.NavigationHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrameIt.General" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
            d:DesignHeight="70" d:DesignWidth="1000"
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}"
             >
    <Grid Grid.Row="0" Margin="20 10 20 10" Name="StatusBar">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Image Source="/Images/Back.png" Grid.Column="0"/>
        <Label Name="AccountName" Content="{Binding Name, UpdateSourceTrigger=PropertyChanged, FallbackValue=AccName}" 
           Grid.Column="1" Style="{StaticResource BaseLabelStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 0 10 0"/>
        <!-- <Image Name="AccountImage" Source="{Binding AvatarImage, UpdateSourceTrigger=PropertyChanged, FallbackValue=Images/Liara.jpg}" 
           Grid.Column="4"/> -->
        <Ellipse Name="AvatarEllipse" Grid.Column="2" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Fill>
                <ImageBrush x:Name="AccountImage" ImageSource="{Binding AvatarImage, UpdateSourceTrigger=PropertyChanged, FallbackValue=Images/GrayLiara.jpg}"/>
            </Ellipse.Fill>
        </Ellipse>
    </Grid>
</UserControl>
