<UserControl x:Class="FrameIt.General.NavigationHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrameIt.General"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="70"
             d:DesignWidth="1000"
             Height="70"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Visibility="{Binding ShowNavigation, Converter={StaticResource BoolToVis}}">

    <!-- OUTER BORDER WITH BOTTOM LINE -->
    <Border BorderBrush="white"
            BorderThickness="0,0,0,1">

        <Grid Margin="20 10 20 10"
              Name="StatusBar">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Source="/Images/Back.png" 
                   Name="GoBackControl"
                   Grid.Column="0" 
                   MouseLeftButtonDown="GoBack_Click"
                   Visibility="{Binding CanGoBack, Converter={StaticResource BoolToVis}}"/>

            <Grid Grid.Column="1"
                  Name="AccountBar"
                  Visibility="{Binding IsAccountBarVisible}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <Label Name="AccountName"
                       MouseLeftButtonDown="Account_Click"
                       Content="{Binding CurrAccount.Name, FallbackValue=AccName}"
                       Grid.Column="0"
                       Style="{StaticResource BaseLabelStyle}"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Margin="0 0 10 0"/>

                <Ellipse Name="AvatarEllipse"
                         MouseLeftButtonDown="Account_Click"
                         Grid.Column="1"
                         Width="50"
                         Height="50"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding CurrAccount.AvatarImage, FallbackValue=Images/GrayLiara.jpg}"/>
                    </Ellipse.Fill>
                </Ellipse>
            </Grid>
        </Grid>
    </Border>
</UserControl>
