<Page x:Class="FrameIt.Account.LoginHome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FrameIt.Account"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{DynamicResource MaterialDesignPaper}"
      Title="LoginHome">

    <Page.Resources>
        <Style x:Key="IndigoFlatButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Foreground" Value="#3F51B5"/>
            <Setter Property="Background" Value="#663F51B5"/>
            <Setter Property="Opacity" Value="0.95"/>
            <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="True"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="Background" Value="#803F51B5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoginInputStyle" TargetType="Control">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="#3F51B5"/>
            <Setter Property="materialDesign:HintAssist.Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="materialDesign:HintAssist.Foreground" Value="#3F51B5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Image Source="/Images/FrameItLogoPng.png" 
               Width="160" Height="160" Margin="0,0,0,20"
               RenderOptions.BitmapScalingMode="HighQuality"/>

        <TextBlock Text="FrameIt" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="10"/>
        <TextBlock Text="Manage all your frames at ease, in one place!" FontSize="24" Foreground="White" HorizontalAlignment="Center" Margin="10,0,10,30"/>

        <StackPanel Name="LoginOrRegister" Visibility="{Binding IsLoginOrRegisterVisible, Converter={StaticResource BoolToVis}, FallbackValue=Visible}">
            <TextBlock Text="Please enter your e-mail to log-in or register:" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="10"/>

            <Grid Width="300" Margin="0,25,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="EmailBox" 
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Email Address"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         CaretBrush="#3F51B5"
                         FontSize="16"
                         Height="45"
                         Margin="0,0,10,0"
                         VerticalAlignment="Bottom">
                    <TextBox.Foreground>
                        <SolidColorBrush Color="White"/>
                    </TextBox.Foreground>
                    <TextBox.Resources>
                        <Style TargetType="TextBox">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="materialDesign:HintAssist.Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter Property="materialDesign:HintAssist.Foreground" Value="#3F51B5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <Button Name="LogInOrRegister_Continue" 
                        Grid.Column="1"
                        Content="Continue" 
                        Command="{Binding LogInOrRegisterCommand}"
                        Width="85" Height="34" FontSize="14"
                        VerticalAlignment="Bottom"/>
            </Grid>
        </StackPanel>

        <StackPanel Name="LogIn" Visibility="{Binding IsLoginVisible, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}" Width="300">
            <TextBlock Text="Please enter your password:" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="10"/>

            <PasswordBox x:Name="LoginPassBox" 
                         PasswordChanged="LoginPassBox_PasswordChanged" 
                         materialDesign:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         CaretBrush="#3F51B5"
                         FontSize="16"
                         Height="45"
                         Margin="0,25,0,20"
                         VerticalAlignment="Bottom">
                <PasswordBox.Foreground>
                    <SolidColorBrush Color="White"/>
                </PasswordBox.Foreground>
                <PasswordBox.Resources>
                    <Style TargetType="PasswordBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="materialDesign:HintAssist.Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="materialDesign:HintAssist.Foreground" Value="#3F51B5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <CheckBox Content="Remember me" 
                          IsChecked="{Binding RememberMe}" 
                          Style="{StaticResource MaterialDesignCheckBox}"
                          FontSize="15"
                          Padding="3"
                          Foreground="#3F51B5"
                          VerticalAlignment="Center"/>

                <Button Content="Log In" Grid.Column="1" Width="85" Height="34" FontSize="14" Click="LogIn_Continue_Click"/>
            </Grid>

            <Button Content="Back to email" 
                    Click="BackToEmail_Click" 
                    FontSize="15"
                    Margin="0,35,0,0"
                    Style="{StaticResource IndigoFlatButtonStyle}"/>
        </StackPanel>
    </StackPanel>
</Page>